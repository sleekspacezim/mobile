import { backEndUrl } from "../../../Utils/Constants";

const baseRouteName = "/property/media";

export const propertyMediaRoutes = {
  postGetDeleteAndUpdatePropertyMedia: `${backEndUrl}${baseRouteName}`,
};