import { backEndUrl } from "../../../Utils/Constants";

const baseRouteName = "/property/location";

export const propertyLocationRoutes = {
  getAndUpdatePropertyLocation: `${backEndUrl}${baseRouteName}`,
};