import { StyleSheet } from "react-native";

export const propertyTypeStyles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    width: "100%",
    paddingBottom: 10,
  },
});

export const rowContainerStyles = StyleSheet.create({
  rowContainer:{
    gap:7,
    alignItems:"center"
  }
})

export const iconSize = 25;