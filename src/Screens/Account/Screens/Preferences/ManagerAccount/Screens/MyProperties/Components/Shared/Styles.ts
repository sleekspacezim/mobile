import { StyleSheet } from "react-native";

export const myPropertyTypeStyles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    width: "100%",
    paddingBottom: 10,
    marginTop:-5
  },
});